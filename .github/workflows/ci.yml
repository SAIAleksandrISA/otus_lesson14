name: C++ CI (G++)

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Compile
      run: g++ -std=c++17 -Wall -Wextra -pedantic -Werror main.cpp workers.cpp -o taskqueue -pthread